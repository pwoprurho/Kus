Testing raw generation with message: Generate the full STEM simulation code for this design: A simple ball falling from 10 meters height using gravity.
--- RAW RESPONSE START ---
```json
{
    "title": "Ball Drop Simulation",
    "description": "A simple simulation demonstrating a ball falling from 10 meters height under gravity, showcasing gravitational acceleration and free fall principles. The simulation includes interactive controls for gravity and the ball's mass, along with a visual path trace.",
    "concept": "Gravitational Acceleration, Free Fall, Newton's Second Law",
    "threejs_code": "(() => {\n    const scene = new THREE.Scene();\n    const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);\n    camera.position.set(5, 12, 15);\n    camera.lookAt(0, 5, 0);\n\n    const renderer = new THREE.WebGLRenderer({ antialias: true });\n    renderer.setSize(window.innerWidth, window.innerHeight);\n    renderer.setClearColor(0x222222);\n    renderer.shadowMap.enabled = true;\n    renderer.shadowMap.type = THREE.PCFSoftShadowMap;\n    document.body.appendChild(renderer.domElement);\n\n    // Cannon-es World\n    const world = new CANNON.World();\n    world.gravity.set(0, -9.81, 0);\n    world.broadphase = new CANNON.SAPBroadphase(world);\n\n    // Lights\n    const ambientLight = new THREE.AmbientLight(0x404040, 0.6);\n    scene.add(ambientLight);\n\n    const dirLight = new THREE.DirectionalLight(0xffffff, 1.2);\n    dirLight.position.set(10, 20, 10);\n    dirLight.castShadow = true;\n    dirLight.shadow.mapSize.width = 2048;\n    dirLight.shadow.mapSize.height = 2048;\n    dirLight.shadow.camera.near = 0.5;\n    dirLight.shadow.camera.far = 50;\n    dirLight.shadow.camera.left = -20;\n    dirLight.shadow.camera.right = 20;\n    dirLight.shadow.camera.top = 20;\n    dirLight.shadow.camera.bottom = -20;\n    scene.add(dirLight);\n\n    // Ground\n    const groundShape = new CANNON.Plane();\n    const groundBody = new CANNON.Body({\n        mass: 0,\n        shape: groundShape,\n        material: new CANNON.Material(),\n        collisionFilterGroup: 1, // ground\n        collisionFilterMask: -1 // Collide with everything\n    });\n    groundBody.quaternion.setFromEuler(-Math.PI / 2, 0, 0); // Rotate to be horizontal\n    world.addBody(groundBody);\n\n    const groundMaterial = new THREE.MeshStandardMaterial({ color: 0x808080, roughness: 0.8, metalness: 0.2 });\n    const groundMesh = new THREE.Mesh(new THREE.PlaneGeometry(50, 50), groundMaterial);\n    groundMesh.rotation.x = -Math.PI / 2;\n    groundMesh.receiveShadow = true;\n    scene.add(groundMesh);\n\n    // Ball\n    const ballRadius = 1;\n    const initialHeight = 10;\n    const ballShape = new CANNON.Sphere(ballRadius);\n    const ballBody = new CANNON.Body({\n        mass: 1,\n        position: new CANNON.Vec3(0, initialHeight, 0),\n        shape: ballShape,\n        material: new CANNON.Material(),\n        collisionFilterGroup: 2, // ball\n        collisionFilterMask: 1 // Collide only with ground\n    });\n    world.addBody(ballBody);\n\n    const ballMaterial = new THREE.MeshStandardMaterial({ color: 0xff4500, roughness: 0.6, metalness: 0.3 });\n    const ballMesh = new THREE.Mesh(new THREE.SphereGeometry(ballRadius, 32, 32), ballMaterial);\n    ballMesh.castShadow = true;\n    ballMesh.receiveShadow = true; // Ball can receive shadows from itself or other objects\n    scene.add(ballMesh);\n\n    // Collision material for ground and ball\n    const groundBallContactMaterial = new CANNON.ContactMaterial(\n        groundBody.material,\n        ballBody.material,\n        { friction: 0.4, restitution: 0.6 }\n    );\n    world.addContactMaterial(groundBallContactMaterial);\n\n    // Grid Helper\n    const gridHelper = new THREE.GridHelper(20, 20, 0x444444, 0x888888);\n    scene.add(gridHelper);\n\n    // Path Trace\n    const pathPoints = [];\n    const pathGeometry = new THREE.BufferGeometry();\n    const pathMaterial = new THREE.LineBasicMaterial({ color: 0x00ff00 });\n    const pathLine = new THREE.Line(pathGeometry, pathMaterial);\n    scene.add(pathLine);\n\n    let lastTime = performance.now();\n\n    function animate() {\n        requestAnimationFrame(animate);\n\n        const currentTime = performance.now();\n        const deltaTime = (currentTime - lastTime) / 1000; // Convert to seconds\n        lastTime = currentTime;\n\n        // Update Cannon-es world\n        world.step(1 / 60, deltaTime, 3);\n\n        // Synchronize Three.js mesh with Cannon-es body\n        ballMesh.position.copy(ballBody.position);\n        ballMesh.quaternion.copy(ballBody.quaternion);\n\n        // Update path trace\n        if (pathPoints.length === 0 || ballBody.position.distanceTo(pathPoints[pathPoints.length - 1]) > 0.1) {\n            pathPoints.push(ballBody.position.clone());\n            const positions = new Float32Array(pathPoints.length * 3);\n            for (let i = 0; i < pathPoints.length; i++) {\n                positions[i * 3] = pathPoints[i].x;\n                positions[i * 3 + 1] = pathPoints[i].y;\n                positions[i * 3 + 2] = pathPoints[i].z;\n            }\n            pathGeometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));\n            pathGeometry.setDrawRange(0, pathPoints.length);\n        }\n\n        renderer.render(scene, camera);\n    }\n\n    animate();\n\n    // Handle window resize\n    window.addEventListener('resize', () => {\n        camera.aspect = window.innerWidth / window.innerHeight;\n        camera.updateProjectionMatrix();\n        renderer.setSize(window.innerWidth, window.innerHeight);\n    });\n\n    return {\n        updateGravity: (x, y, z) => {\n            world.gravity.set(x, y, z);\n        },\n        updateObjectParameter: (index, param, value) => {\n            if (index === 0 && param === 'mass') {\n                ballBody.mass = value;\n                ballBody.updateMassProperties();\n            }\n        },\n        reset: () => {\n            ballBody.position.set(0, initialHeight, 0);\n            ballBody.velocity.set(0, 0, 0);\n            ballBody.angularVelocity.set(0, 0, 0);\n            pathPoints.length = 0; // Clear path\n            pathGeometry.setAttribute('position', new THREE.BufferAttribute(new Float32Array(0), 3));\n            pathGeometry.setDrawRange(0, 0);\n        }\n    };\n})()",
    "config": {
        "gravity": [0, -9.81, 0],
        "timeScale": 1.0
    },
    "objects": [
        {
            "name": "Falling Ball",
            "adjustable": {
                "mass": {
                    "min": 0.1,
                    "max": 10,
                    "value": 1,
                    "step": 0.1,
                    "unit": "kg"
                }
            }
        }
    ],
    "parameters": {}
}
```
--- RAW RESPONSE END ---
Found JSON block. Attempting to parse...
Successfully parsed JSON from block.
