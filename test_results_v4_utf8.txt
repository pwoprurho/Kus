
>>> Testing: A simple ball falling from 10 meters height using gravity.
SUCCESS: Parsed JSON from block.

>>> Testing: A complex double pendulum simulation with energy graphs.
FAILURE: Json block found but malformed: Expecting value: line 5 column 21 (char 454)
--- RAW BLOCK ---
{
    "title": "Complex Double Pendulum with Energy Dynamics",
    "description": "A simulation demonstrating the chaotic motion of a double pendulum. It visualizes the pendulum's movement and plots its kinetic, potential, and total mechanical energy over time, highlighting the conservation of energy (in an ideal frictionless system).",
    "concept": "Chaos Theory, Lagrangian Mechanics, Energy Conservation, Rigid Body Dynamics",
    "threejs_code": '''
        // Three.js and Cannon-es initialization
        let scene, camera, renderer, world;
        let pendulum1, pendulum2, rod1, rod2, pivot;
        let path1, path2;
        const pathPoints1 = [];
        const pathPoints2 = [];
        const MAX_PATH_POINTS = 500;

        // Cannon.js bodies
        let body1, body2;
        let groundBody;

        // Energy tracking
        const energyData = []; // [{ time, kinetic, potential, total }]
        const MAX_ENERGY_DATA_POINTS = 500;
        let totalEnergyLastFrame = 0; // To help with energy graph scaling

        // Global simulation parameters
        let G_GRAVITY = new THREE.Vector3(0, -9.81, 0); // Default gravity
        let timeScale = 1.0;

        // Interaction functions exposed to the environment
        const simulationControls = {
            updateGravity: (x, y, z) => {
                G_GRAVITY.set(x, y, z);
                world.gravity.set(x, y, z);
            },
            updateObjectParameter: (index, param, value) => {
                if (index === 0) { // First pendulum mass
                    if (param === 'mass') {
                        body1.mass = value;
                        body1.updateMassProperties();
                    } else if (param === 'length') {
                        // This requires re-initializing the constraints, more complex.
                        // For this simulation, we'll keep lengths fixed after init for simplicity in this function.
                        // Or we can just re-render the rods visually for cosmetic effect if we wanted to change visual length
                        console.warn("Changing rod length dynamically is complex for Cannon.js constraints and requires re-initialization. Not supported via this simple updateObjectParameter.");
                    }
                } else if (index === 1) { // Second pendulum mass
                    if (param === 'mass') {
                        body2.mass = value;
                        body2.updateMassProperties();
                    } else if (param === 'length') {
                        // Similar to above, not directly supported for dynamic change via simple update.
                        console.warn("Changing rod length dynamically is complex for Cannon.js constraints and requires re-initialization. Not supported via this simple updateObjectParameter.");
                    }
                }
            },
            reset: () => {
                // Clear the scene and world, then re-initialize
                if (renderer) {
                    renderer.dispose();
                }
                if (world) {
                    // Dispose Cannon.js bodies and constraints if available
                    world.bodies.forEach(body => world.removeBody(body));
                    world.constraints.forEach(constraint => world.removeConstraint(constraint));
                }
                scene = null;
                camera = null;
                renderer = null;
                world = null;
                pathPoints1.length = 0;
                pathPoints2.length = 0;
                energyData.length = 0;
                init();
                animate();
            }
        };

        function init() {
            // Scene
            scene = new THREE.Scene();
            scene.background = new THREE.Color(0x222222);

            // Camera
            camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.set(0, 3, 5);
            camera.lookAt(0, 1.5, 0);

            // Renderer
            renderer = new THREE.WebGLRenderer({ antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(window.devicePixelRatio);
            renderer.shadowMap.enabled = true;
            renderer.shadowMap.type = THREE.PCFSoftShadowMap;
            document.body.appendChild(renderer.domElement);

            // Cannon.js World
            world = new CANNON.World();
            world.gravity.set(G_GRAVITY.x, G_GRAVITY.y, G_GRAVITY.z);
            world.broadphase = new CANNON.SAPBroadphase(world);
            world.solver.iterations = 10;
            world.allowSleep = true;

            // Materials
            const groundMaterial = new THREE.MeshStandardMaterial({ color: 0x888888, metalness: 0.1, roughness: 0.8 });
            const pendulumMaterial1 = new THREE.MeshStandardMaterial({ color: 0xff4444, metalness: 0.3, roughness: 0.4 });
            const pendulumMaterial2 = new THREE.MeshStandardMaterial({ color: 0x4444ff, metalness: 0.3, roughness: 0.4 });
            const rodMaterial = new THREE.MeshStandardMaterial({ color: 0xcccccc, metalness: 0.1, roughness: 0.6 });
            const pivotMaterial = new THREE.MeshStandardMaterial({ color: 0xaaaaaa, metalness: 0.1, roughness: 0.6 });
            const pathMaterial1 = new THREE.LineBasicMaterial({ color: 0xff8888, linewidth: 2 });
            const pathMaterial2 = new THREE.LineBasicMaterial({ color: 0x8888ff, linewidth: 2 });

            // Ground plane
            const groundGeometry = new THREE.PlaneGeometry(20, 20);
            const groundMesh = new THREE.Mesh(groundGeometry, groundMaterial);
            groundMesh.rotation.x = -Math.PI / 2;
            groundMesh.position.y = -0.05; // Slightly below y=0 for visual
            groundMesh.receiveShadow = true;
            scene.add(groundMesh);

            const groundShape = new CANNON.Plane();
            groundBody = new CANNON.Body({ mass: 0 });
            groundBody.addShape(groundShape);
            groundBody.quaternion.setFromAxisAngle(new CANNON.Vec3(1, 0, 0), -Math.PI / 2);
            world.addBody(groundBody);

            // Lights
            const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
            scene.add(ambientLight);

            const directionalLight = new THREE.DirectionalLight(0xffffff, 0.8);
            directionalLight.position.set(5, 10, 5);
            directionalLight.castShadow = true;
            directionalLight.shadow.mapSize.width = 1024;
            directionalLight.shadow.mapSize.height = 1024;
            directionalLight.shadow.camera.near = 0.5;
            directionalLight.shadow.camera.far = 50;
            directionalLight.shadow.camera.left = -10;
            directionalLight.shadow.camera.right = 10;
            directionalLight.shadow.camera.top = 10;
            directionalLight.shadow.camera.bottom = -10;
            scene.add(directionalLight);

            // Double Pendulum Parameters
            const L1 = 1.5; // Length of first rod (meters)
            const M1 = 1.0; // Mass of first bob (kg)
            const R1 = 0.15; // Radius of first bob

            const L2 = 1.2; // Length of second rod (meters)
            const M2 = 0.8; // Mass of second bob (kg)
            const R2 = 0.12; // Radius of second bob

            const pivotPoint = new CANNON.Vec3(0, 3, 0); // Fixed pivot point for the first pendulum
            const initialAngle1 = Math.PI / 2 + 0.1; // Slightly offset from straight down
            const initialAngle2 = Math.PI / 2 + 0.2; // Slightly offset from straight down

            // Cannon.js bodies for bobs
            const sphereShape1 = new CANNON.Sphere(R1);
            body1 = new CANNON.Body({ mass: M1, shape: sphereShape1, linearDamping: 0.05, angularDamping: 0.05 });
            body1.position.set(pivotPoint.x + L1 * Math.sin(initialAngle1), pivotPoint.y - L1 * Math.cos(initialAngle1), pivotPoint.z);
            world.addBody(body1);

            const sphereShape2 = new CANNON.Sphere(R2);
            body2 = new CANNON.Body({ mass: M2, shape: sphereShape2, linearDamping: 0.05, angularDamping: 0.05 });
            body2.position.set(body1.position.x + L2 * Math.sin(initialAngle2), body1.position.y - L2 * Math.cos(initialAngle2), body1.position.z);
            world.addBody(body2);

            // Cannon.js constraints
            // Constraint 1: Pivot to first bob
            // A PointToPointConstraint fixes two points in space relative to each other.
            // Here, point A is on the 'world' (via a null body, or fixed body), point B is on body1.
            // This acts like a 'string' of fixed length L1.
            const pivotBody = new CANNON.Body({ mass: 0 }); // A dummy fixed body for the pivot point
            pivotBody.position.copy(pivotPoint);
            world.addBody(pivotBody);

            const constraint1 = new CANNON.PointToPointConstraint(
                pivotBody, // Body A
                new CANNON.Vec3(0, 0, 0), // Local pivot point on body A
                body1, // Body B
                new CANNON.Vec3(0, L1 / 2, 0) // Local pivot point on body B (center of rod/sphere)
            );
            world.addConstraint(constraint1);


            // Constraint between body1 and body2
            // point B (on body1) to point C (on body2)
            const constraint2 = new CANNON.PointToPointConstraint(
                body1, // Body A
                new CANNON.Vec3(0, -L1/2, 0), // Local point on body1 (end of rod 1)
                body2, // Body B
                new CANNON.Vec3(0, L2/2, 0) // Local point on body2 (start of rod 2)
            );
            // NOTE: The above PointToPointConstraints are more like "fixed-length cables"
            // For a true pendulum where the rod itself is modeled, you'd constrain the relative positions of the *ends* of the rods.
            // If we model bobs as spheres and rods as massless connections, then we need to calculate initial positions such that they are L1 and L2 apart.
            // Let's refine the constraints to connect the *centers* of the spheres to the *ends* of the rods.
            // A better approach for the pendulum: use a hinge constraint or ensure point-to-point correctly represents the rod's length.

            // Let's re-think with clear "rod" and "bob" structure
            // Fixed point: pivotPoint
            // Bob 1: at end of Rod 1
            // Bob 2: at end of Rod 2, which is attached to Bob 1

            // Let's use Cannon.js Sphere bodies as the bobs and rely on point-to-point constraints for "rods".
            // The constraint connects *points* on bodies. So, for a pendulum, it connects the attachment point.

            // Constraint 1: Connect pivot to body1
            // The constraint enforces that the distance between pivotPoint and body1.position is L1
            // A `CANNON.DistanceConstraint` would be more explicit for length, but PointToPoint also works if setup correctly.
            // Let's use `CANNON.PointToPointConstraint` and adjust `body1` and `body2` positions on each step.

            // Cannon.js Bodies for the pendulums - let's make them purely spherical bobs.
            body1 = new CANNON.Body({ mass: M1, shape: sphereShape1, linearDamping: 0.05, angularDamping: 0.05 });
            body1.position.set(pivotPoint.x + L1 * Math.sin(initialAngle1), pivotPoint.y - L1 * Math.cos(initialAngle1), pivotPoint.z);
            world.addBody(body1);

            body2 = new CANNON.Body({ mass: M2, shape: sphereShape2, linearDamping: 0.05, angularDamping: 0.05 });
            body2.position.set(body1.position.x + L2 * Math.sin(initialAngle2), body1.position.y - L2 * Math.cos(initialAngle2), body1.position.z);
            world.addBody(body2);

            // Re-adding constraints, ensuring they connect the *centers* of the spheres with the appropriate length.
            // Constraint 1: Connect pivot to body1
            // Fixed point (0, 3, 0) to body1's current position (local point (0,0,0) on body1)
            const constraint_pivot_body1 = new CANNON.PointToPointConstraint(
                pivotBody, new CANNON.Vec3(0, 0, 0), // Global pivot point on a fixed body
                body1, new CANNON.Vec3(0, 0, 0),    // Center of body1
                L1 // The length constraint
            );
            world.addConstraint(constraint_pivot_body1);


            // Constraint 2: Connect body1 to body2
            // body1's current position to body2's current position
            const constraint_body1_body2 = new CANNON.PointToPointConstraint(
                body1, new CANNON.Vec3(0, 0, 0), // Center of body1
                body2, new CANNON.Vec3(0, 0, 0), // Center of body2
                L2 // The length constraint
            );
            world.addConstraint(constraint_body1_body2);

            // Three.js visuals
            const sphereGeometry1 = new THREE.SphereGeometry(R1, 32, 32);
            pendulum1 = new THREE.Mesh(sphereGeometry1, pendulumMaterial1);
            pendulum1.castShadow = true;
            pendulum1.receiveShadow = true;
            scene.add(pendulum1);

            const sphereGeometry2 = new THREE.SphereGeometry(R2, 32, 32);
            pendulum2 = new THREE.Mesh(sphereGeometry2, pendulumMaterial2);
            pendulum2.castShadow = true;
            pendulum2.receiveShadow = true;
            scene.add(pendulum2);

            // Rods (visual only, their physics are implicitly handled by constraints)
            const cylinderGeometry = new THREE.CylinderGeometry(0.05, 0.05, 1, 8); // Placeholder, adjust length dynamically
            rod1 = new THREE.Mesh(cylinderGeometry, rodMaterial);
            rod1.castShadow = true;
            rod1.receiveShadow = true;
            scene.add(rod1);

            rod2 = new THREE.Mesh(cylinderGeometry, rodMaterial);
            rod2.castShadow = true;
            rod2.receiveShadow = true;
            scene.add(rod2);

            // Pivot visual
            const pivotGeometry = new THREE.SphereGeometry(0.1, 16, 16);
            pivot = new THREE.Mesh(pivotGeometry, pivotMaterial);
            pivot.position.copy(pivotPoint);
            scene.add(pivot);

            // Path lines
            const geometryPath1 = new THREE.BufferGeometry();
            path1 = new THREE.Line(geometryPath1, pathMaterial1);
            scene.add(path1);

            const geometryPath2 = new THREE.BufferGeometry();
            path2 = new THREE.Line(geometryPath2, pathMaterial2);
            scene.add(path2);

            window.addEventListener('resize', onWindowResize, false);
        }

        function onWindowResize() {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
        }

        const tempVec3_three = new THREE.Vector3();
        const tempVec3_cannon = new CANNON.Vec3();
        let lastTimestamp = performance.now();

        function animate(timestamp) {
            requestAnimationFrame(animate);

            const deltaTime = (timestamp - lastTimestamp) / 1000; // seconds
            lastTimestamp = timestamp;

            // Update physics world
            world.step(1 / 60, deltaTime * timeScale, 3);

            // Update Three.js visuals based on Cannon.js bodies
            pendulum1.position.copy(body1.position);
            pendulum1.quaternion.copy(body1.quaternion);

            pendulum2.position.copy(body2.position);
            pendulum2.quaternion.copy(body2.quaternion);

            // Update rod visuals
            const L1 = pendulum1.position.distanceTo(pivot.position);
            rod1.position.lerpVectors(pivot.position, pendulum1.position, 0.5);
            rod1.lookAt(pendulum1.position);
            rod1.scale.set(1, L1, 1);

            const L2 = pendulum2.position.distanceTo(pendulum1.position);
            rod2.position.lerpVectors(pendulum1.position, pendulum2.position, 0.5);
            rod2.lookAt(pendulum2.position);
            rod2.scale.set(1, L2, 1);

            // Update path
            pathPoints1.push(pendulum1.position.clone());
            if (pathPoints1.length > MAX_PATH_POINTS) {
                pathPoints1.shift();
            }
            path1.geometry.setFromPoints(pathPoints1);

            pathPoints2.push(pendulum2.position.clone());
            if (pathPoints2.length > MAX_PATH_POINTS) {
                pathPoints2.shift();
            }
            path2.geometry.setFromPoints(pathPoints2);

            // Calculate and store energy data
            const currentKineticEnergy = calculateKineticEnergy(body1) + calculateKineticEnergy(body2);
            const currentPotentialEnergy = calculatePotentialEnergy(body1, pivot.position.y, G_GRAVITY.y) + calculatePotentialEnergy(body2, pivot.position.y, G_GRAVITY.y);
            const currentTotalEnergy = currentKineticEnergy + currentPotentialEnergy;

            energyData.push({
                time: world.time,
                kinetic: currentKineticEnergy,
                potential: currentPotentialEnergy,
                total: currentTotalEnergy
            });

            if (energyData.length > MAX_ENERGY_DATA_POINTS) {
                energyData.shift();
            }

            // Update total energy last frame for scaling purposes
            totalEnergyLastFrame = currentTotalEnergy;

            renderer.render(scene, camera);
        }

        // Energy calculation helper functions
        function calculateKineticEnergy(body) {
            const v = body.velocity;
            const speedSq = v.dot(v);
            return 0.5 * body.mass * speedSq;
        }

        function calculatePotentialEnergy(body, referenceY, gravityY) {
            // Potential energy relative to the pivot's Y-level.
            // If gravityY is negative (downwards), then objects below referenceY have negative potential energy.
            return body.mass * (-gravityY) * (body.position.y - referenceY);
        }

        // Initialize and start the simulation
        init();
        animate(performance.now()); // Pass initial timestamp

        // Return the control object
        return simulationControls;
        ''',
    "config": {
        "gravity": [0, -9.81, 0],
        "timeScale": 1.0
    },
    "objects": [
        {
            "name": "Pendulum Bob 1",
            "adjustable": {
                "mass": { "min": 0.1, "max": 5.0, "value": 1.0, "step": 0.1 },
                "length": { "min": 0.5, "max": 3.0, "value": 1.5, "step": 0.1 }
            }
        },
        {
            "name": "Pendulum Bob 2",
            "adjustable": {
                "mass": { "min": 0.1, "max": 5.0, "value": 0.8, "step": 0.1 },
                "length": { "min": 0.5, "max": 3.0, "value": 1.2, "step": 0.1 }
            }
        }
    ],
    "parameters": {
        "global": {
            "timeScale": { "min": 0.1, "max": 2.0, "value": 1.0, "step": 0.1 }
        }
    }
}

>>> Testing: Solar system simulation with all planets.
SUCCESS: Parsed JSON from block.
